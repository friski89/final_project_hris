version: '3'

services:
    hcis:
        container_name: hcis
        image: hcis
        build:
            context: .
            dockerfile: Dockerfile
        command: 'php artisan serve --port 443 --host 0.0.0.0'
        ports:
            - "443:443"
